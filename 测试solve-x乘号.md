# solve 插件「x / × / ⋅ 乘号」验收用例

> 使用方法：在 Obsidian 中打开此笔记，确保 solve（编辑器内 inline 计算器）已启用并处于开启状态。光标停留到下列每一行时，应该能看到行尾弹出计算结果。

## 一、x 作为乘号的核心场景（本次修复重点）

```
2x3
2 X 3
2 x 3
3x(1+2)
(1+2)x3
2xsin(30)
```

期望结果依次：`6`、`6`、`6`、`9`、`9`、`1`（或 sin(30°)≈0.5 时 1，取决于 solve 的角度模式；数值正确即可）。

## 二、其它 Unicode 乘 / 除号

```
2×3
2⋅3
6÷2
5×4+3⋅2
```

期望结果依次：`6`、`6`、`3`、`26`。

## 三、x 保留为变量名的场景（不能被替换成 *）

```
x=5
y=x+2
3x+2
xy=6
10x
a x b
```

期望行为：
- `x=5` → 识别为赋值 5；
- `y=x+2` → 在 x 已赋值前提下得到 `7`（逐行评估的 solve 插件通常会记住上下文）；
- `3x+2` → 按代数表达式处理（不是 `3*+2` 报错）；
- `xy=6` / `10x` / `a x b` → 保留 x、xy 等标识符含义，不出现语法错误。

## 四、隐式乘法与全角数字

```
4(x+2)
３×５
１２３x２
```

期望结果：（x=5 上下文）`28`、`15`、`246`。

## 五、对照样例

如「2x3」行结果仍然不显示（只显示灰色无结果或报错），说明修复未生效：
1. 请在 Obsidian 插件面板 → 找到 solve → 点击「Reload 插件」（或直接重启 Obsidian）；
2. 再回到本笔记重试。
