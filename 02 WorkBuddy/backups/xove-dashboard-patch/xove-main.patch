--- main.js.orig	2026-09-04 18:45:11
+++ main.js.patched	2026-09-04 20:36:52
@@ -931,12 +931,16 @@
     /* 人生打卡清单（独立顶栏整页）：读取一个 md，按一级标题(#)分组，展示并管理任务 */
     checklistEnabled: true,
     checklistTitle: '人生打卡清单',
+    customBoards: [],
+    customBoardsFolder: '面板',
     checklistFile: '01 主页/人生打卡清单.README.md',
+    planBoards: [],
+    planBoardsFolder: '计划任务',
     checklistAppendDate: false,
     checklistOutlineFile: '',
     checklistAutoGroup: false,
     importEnabled: true,
-    importTitle: '导入',
+    importTitle: '新建',
     inspirationLimit: 20,
     memoRecall: { enabled: false, range: '30d', count: 3 },
     heatmapMode: 'year',
@@ -3693,6 +3697,17 @@
 }
 
 /** 人生打卡清单（独立顶栏整页）：读取一个 md，按一级标题(#)分组，展示并管理任务。 */
+/** 模糊搜索选择库内任意 md（清单「导入 md」等场景复用）。 */
+class FilePickModal extends obsidian.FuzzySuggestModal {
+    constructor(app, onPick) {
+        super(app);
+        this.onPick = onPick;
+        this.setPlaceholder('选择要导入为清单的 md 文件…');
+    }
+    getItems() { return this.app.vault.getMarkdownFiles(); }
+    getItemText(f) { return f.path; }
+    onChooseItem(f) { this.onPick(f.path); }
+}
 class ChecklistBoard {
     constructor(host) {
         this.host = host;
@@ -3715,6 +3730,15 @@
             return '';
         return raw.toLowerCase().endsWith('.md') ? raw : raw + '.md';
     }
+    /** 更换清单数据源：选择库内任意 md，按其标题结构自动分组（顺序 = 文档标题顺序） */
+    async importSourceFile(path) {
+        const s = this.host.plugin.settings;
+        s.checklistFile = path;
+        await this.host.plugin.saveSettings();
+        this.selectedGroup = 'all';
+        this.host.showToast('清单数据源已切换：' + path);
+        await this.show();
+    }
     async loadGroups() {
         const p = this.path();
         if (!p)
@@ -3775,7 +3799,7 @@
         const groups = [];
         let cur = { heading: '', headingLineNo: -1, tasks: [] };
         lines.forEach((line, i) => {
-            const h = line.match(/^#\s+(.*)$/);
+            const h = line.match(/^#{1,6}\s+(.*)$/);
             if (h) {
                 if (cur.heading !== '' || cur.tasks.length)
                     groups.push(cur);
@@ -3816,14 +3840,14 @@
         const lines = (await this.host.app.vault.read(file)).split(/\r?\n/);
         let insertAt = lines.length;
         if (heading) {
-            const hi = lines.findIndex((l) => /^#\s+/.test(l) && l.replace(/^#\s+/, '').trim() === heading);
+            const hi = lines.findIndex((l) => /^#{1,6}\s+/.test(l) && l.replace(/^#{1,6}\s+/, '').trim() === heading);
             if (hi >= 0) {
                 insertAt = hi + 1;
-                while (insertAt < lines.length && !/^#\s+/.test(lines[insertAt]))
+                while (insertAt < lines.length && !/^#{1,6}\s+/.test(lines[insertAt]))
                     insertAt++;
             }
             else {
-                lines.push('', '# ' + heading);
+                lines.push('', '## ' + heading);
                 insertAt = lines.length;
             }
         }
@@ -3850,9 +3874,10 @@
         const lines = (await this.host.app.vault.read(file)).split(/\r?\n/);
         if (lineNo < 0 || lineNo >= lines.length)
             return;
-        if (!/^#\s+/.test(lines[lineNo]))
+        const hm = /^(#{1,6})\s+/.exec(lines[lineNo]);
+        if (!hm)
             return;
-        lines[lineNo] = '# ' + newHeading;
+        lines[lineNo] = hm[1] + ' ' + newHeading;
         await this.host.app.vault.modify(file, lines.join('\n'));
     }
     /** 重命名任务：保留勾选状态与行尾 ✅日期，只替换正文 */
@@ -3971,6 +3996,11 @@
                 input.value = '';
             }
         });
+        const importBtn = bar.createEl('button', { cls: 'ck-btn', text: '导入 md' });
+        importBtn.title = '选择库内任意 md 作为清单数据源（按其标题自动分类）';
+        importBtn.addEventListener('click', () => {
+            new FilePickModal(this.host.app, (path) => void this.importSourceFile(path)).open();
+        });
         const refreshBtn = bar.createEl('button', { cls: 'ck-btn', text: '↻' });
         refreshBtn.addEventListener('click', () => void this.show());
         for (const g of groups) {
@@ -4008,10 +4038,51 @@
         }
     }
 }
-/** 导入页（第四页）：把任意 md 一键建成首页「笔记映射」卡片。 */
+/** 字数统计缓存有效期（毫秒）：期间直接用缓存值，过期后后台重算。 */
+const WC_TTL = 10 * 60 * 1000;
+/** 计划任务面板：与「人生打卡清单」同款界面（分组 / 勾选写回 / 添加 / 重命名），
+ *  唯一区别是数据源 —— 每个面板绑定自己的 md（可在面板内「导入 md」随时更换）。
+ *  继承复用 ChecklistBoard 全部能力，只覆盖 title / path / show / importSourceFile。 */
+class PlanBoard extends ChecklistBoard {
+    constructor(host) {
+        super(host);
+        this.planId = '';
+    }
+    cfg() { return (this.host.plugin.settings.planBoards ?? []).find((b) => b.id === this.planId); }
+    title() { return this.cfg()?.name || '计划任务'; }
+    path() { return (this.cfg()?.filePath || '').trim(); }
+    async show(id) {
+        this.planId = id;
+        this.selectedGroup = 'all';
+        await super.show();
+        // 与官方清单页区分：避免「禁用清单」等逻辑误伤，也隔离刷新事件
+        this.host.currentPage = 'plan:' + id;
+    }
+    /** 覆盖：导入 md 只改本面板的数据源，不动「人生打卡清单」的 checklistFile */
+    async importSourceFile(path) {
+        const b = this.cfg();
+        if (!b)
+            return;
+        b.filePath = path;
+        await this.host.plugin.saveSettings();
+        this.selectedGroup = 'all';
+        this.host.showToast('已导入：' + path);
+        await this.show(this.planId);
+    }
+}
+/* ============================================================
+   XOVE CUSTOM · 新建页
+   职责：
+     1) 新建「计划任务面板」：与「人生打卡清单」同款的独立导航面板，
+        可导入已有 md 作为数据源，或不选则自动新建一份 md。
+     2) 管理已建面板：排序（顺序即导航顺序）、重新导入 md、移除。
+     3) 次要能力：把已有 md 映射成首页「笔记映射」卡片（折叠 + 懒加载）。
+   ============================================================ */
 class ImportBoard {
     constructor(host) {
         this.host = host;
+        this.mapBuilt = false;
+        this.pickPath = '';
     }
     title() { return this.host.plugin.settings.importTitle || t('settings.importNamePlaceholder'); }
     async show() {
@@ -4031,17 +4102,210 @@
     }
     render(root) {
         root.empty();
+        this.renderHead(root);
+        this.renderPlanCreate(root);
+        this.renderPlanManager(root);
+        this.renderLegacyPanels(root);
+        this.renderMapSection(root);
+        this.renderMirrorList(root);
+    }
+    renderHead(root) {
         const head = root.createDiv({ cls: 'im-head' });
         head.createEl('h2', { cls: 'im-title', text: this.title() });
-        head.createDiv({ cls: 'im-sub', text: t('home.importSub') });
-        const form = root.createDiv({ cls: 'im-form' });
-        // 方式一：从库中选择
+        head.createDiv({ cls: 'im-sub', text: '新建「计划任务面板」：与「人生打卡清单」同款，可导入自己的 md 数据源' });
+    }
+    /** 新建计划任务面板：名称 + 图标 + 数据源（可选导入已有 md） */
+    renderPlanCreate(root) {
+        const box = root.createDiv({ cls: 'im-diary' });
+        box.createEl('h3', { cls: 'im-card__title', text: '新建计划任务面板' });
+        const fields = box.createDiv({ cls: 'im-diary__row im-panel__fields' });
+        const iconInput = fields.createEl('input', { cls: 'im-input im-input--icon', attr: { placeholder: '\u25A3', 'aria-label': '面板图标' } });
+        iconInput.value = '\u25A3';
+        const nameInput = fields.createEl('input', { cls: 'im-input', attr: { placeholder: '面板名称，如：年度计划 / 读书清单' } });
+        const row = box.createDiv({ cls: 'im-diary__row' });
+        const pickBtn = row.createEl('button', { cls: 'im-btn', text: '选择 md 导入' });
+        const pickTip = row.createSpan({ cls: 'im-pick__tip', text: '未选择则自动新建一份 md' });
+        pickBtn.addEventListener('click', () => {
+            new FilePickModal(this.host.app, (path) => {
+                this.pickPath = path;
+                pickTip.textContent = '已选：' + path;
+            }).open();
+        });
+        const ok = row.createEl('button', { cls: 'im-btn im-btn--primary', text: '创建面板' });
+        const run = () => void this.createPlanBoard(nameInput.value.trim(), iconInput.value.trim());
+        ok.addEventListener('click', run);
+        nameInput.addEventListener('keydown', (ev) => {
+            if (ev.key === 'Enter')
+                run();
+        });
+    }
+    async createPlanBoard(name, icon) {
+        if (!name) {
+            this.host.showToast('请输入面板名称', 'error');
+            return;
+        }
+        const s = this.host.plugin.settings;
+        let filePath = this.pickPath;
+        if (!filePath) {
+            const folder = (s.planBoardsFolder || '\u8BA1\u5212\u4EFB\u52A1').trim().replace(/\/+$/, '');
+            const safeName = name.replace(/[*"/<>:|?\\]/g, '-');
+            filePath = (folder ? folder + '/' : '') + safeName + '.md';
+            try {
+                if (folder)
+                    await this.host.ensureFolder(folder);
+                if (!this.host.app.vault.getAbstractFileByPath(filePath))
+                    await this.host.app.vault.create(filePath, '# ' + name + '\n\n- [ ] \u7B2C\u4E00\u6761\u4EFB\u52A1\n');
+            }
+            catch (e) {
+                console.error('[Xove] create plan board failed', e);
+                this.host.showToast('创建失败：' + (e && e.message ? e.message : String(e)), 'error');
+                return;
+            }
+        }
+        if (!Array.isArray(s.planBoards))
+            s.planBoards = [];
+        const id = 'pb' + Date.now();
+        s.planBoards.push({ id, name, icon: icon || '\u25A3', filePath });
+        await this.host.plugin.saveSettings();
+        this.pickPath = '';
+        this.host.showToast('已创建面板：' + name);
+        void this.host.showPlanBoard(id);
+    }
+    /** 计划任务面板管理：排序 / 重新导入 md / 移除 */
+    renderPlanManager(root) {
+        const boards = Array.isArray(this.host.plugin.settings.planBoards) ? this.host.plugin.settings.planBoards : [];
+        if (!boards.length)
+            return;
+        const wrap = root.createDiv({ cls: 'im-section' });
+        const head = wrap.createDiv({ cls: 'im-section__head' });
+        head.createSpan({ cls: 'im-section__title', text: '计划任务面板（顺序即导航顺序）' });
+        const list = wrap.createDiv({ cls: 'im-list im-panels' });
+        boards.forEach((b, i) => {
+            const row = list.createDiv({ cls: 'im-item' });
+            row.createSpan({ cls: 'im-item__icon', text: b.icon || '\u25A3' });
+            row.createSpan({ cls: 'im-item__name', text: b.name });
+            row.createSpan({ cls: 'im-item__path', text: b.filePath });
+            const btns = row.createDiv({ cls: 'im-panels__btns' });
+            const up = btns.createEl('button', { cls: 'im-panels__btn', text: '\u2191' });
+            const down = btns.createEl('button', { cls: 'im-panels__btn', text: '\u2193' });
+            const reimp = btns.createEl('button', { cls: 'im-panels__btn', text: '\u21EA' });
+            const del = btns.createEl('button', { cls: 'im-item__del', text: '\u2715' });
+            up.title = '上移';
+            down.title = '下移';
+            reimp.title = '重新导入 md';
+            del.title = '移除面板（源文件保留）';
+            up.disabled = i === 0;
+            down.disabled = i === boards.length - 1;
+            up.addEventListener('click', () => void this.movePlan(b.id, -1));
+            down.addEventListener('click', () => void this.movePlan(b.id, 1));
+            reimp.addEventListener('click', () => {
+                new FilePickModal(this.host.app, (path) => void this.reimportPlan(b.id, path)).open();
+            });
+            let armed = false;
+            del.addEventListener('click', () => {
+                if (!armed) {
+                    armed = true;
+                    del.textContent = '确认？';
+                    del.addClass('is-danger');
+                    return;
+                }
+                void this.removePlan(b.id);
+            });
+        });
+    }
+    async movePlan(id, dir) {
+        const arr = this.host.plugin.settings.planBoards ?? [];
+        const i = arr.findIndex((b) => b.id === id);
+        const j = i + dir;
+        if (i < 0 || j < 0 || j >= arr.length)
+            return;
+        const [it] = arr.splice(i, 1);
+        arr.splice(j, 0, it);
+        await this.host.plugin.saveSettings();
+        this.host.showToast('顺序已更新');
+        void this.host.showDashboard();
+    }
+    async reimportPlan(id, path) {
+        const b = (this.host.plugin.settings.planBoards ?? []).find((x) => x.id === id);
+        if (!b)
+            return;
+        b.filePath = path;
+        await this.host.plugin.saveSettings();
+        this.host.showToast('已导入：' + path);
+        void this.host.showPlanBoard(id);
+    }
+    async removePlan(id) {
+        const s = this.host.plugin.settings;
+        s.planBoards = (s.planBoards ?? []).filter((x) => x.id !== id);
+        await this.host.plugin.saveSettings();
+        this.host.showToast('已移除面板（源文件保留）');
+        void this.host.showDashboard();
+    }
+    /** 旧版「自定义面板」（整篇 md 渲染）若仍存在则继续可管理 */
+    renderLegacyPanels(root) {
+        const boards = Array.isArray(this.host.plugin.settings.customBoards) ? this.host.plugin.settings.customBoards : [];
+        if (!boards.length)
+            return;
+        const wrap = root.createDiv({ cls: 'im-section' });
+        const head = wrap.createDiv({ cls: 'im-section__head' });
+        head.createSpan({ cls: 'im-section__title', text: '其他面板（旧版整篇 md 渲染）' });
+        const list = wrap.createDiv({ cls: 'im-list im-panels' });
+        boards.forEach((b, i) => {
+            const row = list.createDiv({ cls: 'im-item' });
+            row.createSpan({ cls: 'im-item__icon', text: b.icon || '\u{1F4C1}' });
+            row.createSpan({ cls: 'im-item__name', text: b.name });
+            row.createSpan({ cls: 'im-item__path', text: b.filePath });
+            const btns = row.createDiv({ cls: 'im-panels__btns' });
+            const up = btns.createEl('button', { cls: 'im-panels__btn', text: '\u2191' });
+            const down = btns.createEl('button', { cls: 'im-panels__btn', text: '\u2193' });
+            const del = btns.createEl('button', { cls: 'im-item__del', text: '\u2715' });
+            up.disabled = i === 0;
+            down.disabled = i === boards.length - 1;
+            up.addEventListener('click', () => void this.movePanel(b.id, -1));
+            down.addEventListener('click', () => void this.movePanel(b.id, 1));
+            del.addEventListener('click', () => void this.removePanel(b.id));
+        });
+    }
+    async movePanel(id, dir) {
+        const arr = this.host.plugin.settings.customBoards ?? [];
+        const i = arr.findIndex((b) => b.id === id);
+        const j = i + dir;
+        if (i < 0 || j < 0 || j >= arr.length)
+            return;
+        const [it] = arr.splice(i, 1);
+        arr.splice(j, 0, it);
+        await this.host.plugin.saveSettings();
+        this.host.showToast('顺序已更新');
+        void this.host.showDashboard();
+    }
+    async removePanel(id) {
+        await this.host.removeCustomPanel(id);
+    }
+    /** 次要区块：映射已有笔记（折叠，展开时才构建文件列表） */
+    renderMapSection(root) {
+        const wrap = root.createDiv({ cls: 'im-section' });
+        const head = wrap.createDiv({ cls: 'im-section__head' });
+        head.createSpan({ cls: 'im-section__title', text: '映射已有笔记为首页卡片' });
+        const toggle = head.createEl('button', { cls: 'im-section__toggle', text: '展开' });
+        const body = wrap.createDiv({ cls: 'im-section__body is-hidden' });
+        toggle.addEventListener('click', () => {
+            const willShow = body.hasClass('is-hidden');
+            body.toggleClass('is-hidden', !willShow);
+            toggle.textContent = willShow ? '收起' : '展开';
+            if (willShow)
+                this.buildMapForm(body);
+        });
+    }
+    buildMapForm(body) {
+        if (this.mapBuilt)
+            return;
+        this.mapBuilt = true;
+        const form = body.createDiv({ cls: 'im-form' });
         const sel = form.createEl('select', { cls: 'im-select' });
         sel.createEl('option', { value: '', text: t('home.importPickPlaceholder') });
         const files = this.host.app.vault.getMarkdownFiles().sort((a, b) => a.path.localeCompare(b.path));
         for (const f of files)
             sel.createEl('option', { value: f.path, text: f.path });
-        // 方式二：直接粘贴路径
         const pathInput = form.createEl('input', { cls: 'im-input', attr: { placeholder: t('home.importPathPlaceholder') } });
         sel.addEventListener('change', () => {
             if (sel.value)
@@ -4053,7 +4317,8 @@
         const btnRow = form.createDiv({ cls: 'im-btns' });
         const btn = btnRow.createEl('button', { cls: 'im-btn im-btn--primary', text: t('home.importBtn') });
         btn.addEventListener('click', () => void this.doImport(pathInput.value.trim(), titleInput.value.trim(), iconInput.value.trim()));
-        // 已有映射卡片
+    }
+    renderMirrorList(root) {
         const list = root.createDiv({ cls: 'im-list' });
         list.createEl('h3', { cls: 'im-list__title', text: t('home.importExisting') });
         const mirrors = Array.isArray(this.host.plugin.settings.mirrors) ? this.host.plugin.settings.mirrors : [];
@@ -4067,6 +4332,7 @@
             row.createSpan({ cls: 'im-item__name', text: m.title || m.filePath });
             row.createSpan({ cls: 'im-item__path', text: m.filePath });
             const rm = row.createEl('button', { cls: 'im-item__del', text: '\u2715' });
+            rm.setAttribute('aria-label', '移除映射');
             rm.addEventListener('click', () => void this.removeMirror(m.id));
         }
     }
@@ -4089,15 +4355,9 @@
             return;
         }
         s.mirrors.push({
-            id: 'm' + Date.now(),
-            filePath: p,
-            scope: 'file',
-            heading: '',
-            title: title || file.basename,
-            icon: icon || '\u{1F4C4}',
-            enabled: true,
-            auto: false,
-            sourceId: '',
+            id: 'm' + Date.now(), filePath: p, scope: 'file', heading: '',
+            title: title || file.basename, icon: icon || '\u{1F4C4}',
+            enabled: true, auto: false, sourceId: '',
         });
         await this.host.plugin.saveSettings();
         this.host.showToast(t('home.importDone'));
@@ -4110,7 +4370,66 @@
         await this.show();
     }
 }
-/** 通用看板（第三页）渲染器 — extracted from DashboardView. */
+/** 目标编辑弹窗：字段配置化渲染，便于后续扩展新字段。 */
+class GoalEditModal extends obsidian.Modal {
+    constructor(app, plugin, goal, onSave) {
+        super(app);
+        this.plugin = plugin;
+        this.goal = goal
+            ? { ...goal }
+            : { id: '', name: '', target: 100, current: 0, unit: '', icon: '\u{1F3AF}', color: '#8b5cf6', auto: '', folder: '' };
+        this.onSave = onSave;
+    }
+    onOpen() {
+        const { contentEl } = this;
+        contentEl.empty();
+        const g = this.goal;
+        contentEl.createEl('h2', { text: g.id ? '编辑目标' : '新增目标' });
+        const form = contentEl.createDiv({ cls: 'ad-gform' });
+        const fields = [
+            { key: 'name', label: '名称', type: 'text', ph: '例如：写作字数' },
+            { key: 'target', label: '目标值', type: 'number', ph: '100' },
+            { key: 'unit', label: '单位', type: 'text', ph: '字 / 本 / 天' },
+            { key: 'icon', label: '图标', type: 'text', ph: '\u{1F3AF}', cls: 'ad-gform__icon' },
+            { key: 'color', label: '颜色', type: 'color', ph: '' },
+            { key: 'folder', label: '统计范围', type: 'text', ph: '留空=整个库，或填文件夹如 07 成长' },
+        ];
+        for (const f of fields) {
+            const row = form.createDiv({ cls: 'ad-gform__row' });
+            row.createEl('label', { text: f.label });
+            const input = row.createEl('input', {
+                cls: 'ad-modal-input' + (f.cls ? ' ' + f.cls : ''),
+                attr: { type: f.type, placeholder: f.ph },
+            });
+            input.value = f.type === 'number' ? String(g[f.key] ?? 0) : (g[f.key] || (f.key === 'color' ? '#8b5cf6' : ''));
+            input.addEventListener('input', () => {
+                g[f.key] = f.type === 'number' ? (Number(input.value) || 0) : input.value.trim();
+            });
+        }
+        const rAuto = form.createDiv({ cls: 'ad-gform__row' });
+        rAuto.createEl('label', { text: '自动统计' });
+        const selAuto = rAuto.createEl('select', { cls: 'ad-modal-input' });
+        selAuto.createEl('option', { value: '', text: '手动（± 按钮调整）' });
+        selAuto.createEl('option', { value: 'wordcount', text: '字数统计（自动）' });
+        selAuto.value = g.auto || '';
+        selAuto.addEventListener('change', () => { g.auto = selAuto.value; });
+        const btns = contentEl.createDiv({ cls: 'ad-gform__btns' });
+        const ok = btns.createEl('button', { cls: 'mod-cta', text: '保存' });
+        ok.addEventListener('click', () => {
+            if (!(g.name || '').trim())
+                g.name = '未命名目标';
+            if (!g.id)
+                g.id = 'g' + Date.now();
+            this.close();
+            void this.onSave({ ...g });
+        });
+        const cancel = btns.createEl('button', { text: '取消' });
+        cancel.addEventListener('click', () => this.close());
+    }
+    onClose() {
+        this.contentEl.empty();
+    }
+}
 class OpportunityBoard {
     constructor(host) {
         // Board state
@@ -7490,6 +7809,7 @@
         this.projectBoard = new ProjectBoard(this);
         this.checklistBoard = new ChecklistBoard(this);
         this.importBoard = new ImportBoard(this);
+        this.planBoard = new PlanBoard(this);
     }
     /** Theme actually in effect for the dashboard right now. */
     effectiveTheme() {
@@ -8051,7 +8371,15 @@
         }
         if (this.plugin.settings.checklistEnabled) {
             navItems.push({ glyph: '☑', label: this.plugin.settings.checklistTitle || t('home.nav.checklist'), action: 'checklist', svg: undefined });
+        }
+        // 计划任务面板：与「人生打卡清单」同款，各自绑定一份 md（顺序 = 数组顺序）
+        for (const pb of (this.plugin.settings.planBoards ?? [])) {
+            navItems.push({ glyph: pb.icon || '▣', label: pb.name, action: 'plan:' + pb.id, svg: undefined });
         }
+        // 旧版自定义面板（整篇 md 渲染），若仍存在则继续显示
+        for (const cb of (this.plugin.settings.customBoards ?? [])) {
+            navItems.push({ glyph: cb.icon || '▤', label: cb.name, action: 'custom:' + cb.id, svg: undefined });
+        }
         if (this.plugin.settings.importEnabled) {
             navItems.push({ glyph: '\u21E9', label: this.plugin.settings.importTitle || t('home.nav.import'), action: 'import', svg: undefined });
         }
@@ -8092,6 +8420,10 @@
                         void this.checklistBoard.show();
                     if (it.action === 'import')
                         void this.importBoard.show();
+                    if (it.action.startsWith('plan:'))
+                        void this.showPlanBoard(it.action.slice(5));
+                    if (it.action.startsWith('custom:'))
+                        void this.showCustomPanel(it.action.slice(7));
                     if (it.action === 'homeNote')
                         void this.openHomeNote();
                 }
@@ -11418,14 +11750,14 @@
     renderGoals(board) {
         const card = this.getOrCreateCard(board, 'ad-card ad-b-goals');
         this.cardHead(card, '\u{1F3AF}', t('home.modules.goals'), '');
-        let goals = (this.plugin.settings.goals ?? []).map((g) => ({ ...g }));
-        if (goals.length === 0)
-            goals = DEFAULT_GOALS.map((g) => ({ ...g }));
+        // 仅在配置缺失时回退默认值；用户主动清空后不再复活默认目标
+        const goals = Array.isArray(this.plugin.settings.goals)
+            ? this.plugin.settings.goals.map((g) => ({ ...g }))
+            : DEFAULT_GOALS.map((g) => ({ ...g }));
         const body = card.createDiv({ cls: 'ad-goals' });
-        const saveAndRender = async () => {
+        const commit = async () => {
             this.plugin.settings.goals = goals.map((g) => ({ ...g }));
             await this.plugin.saveSettings();
-            renderList();
         };
         const renderList = () => {
             body.empty();
@@ -11443,20 +11775,143 @@
                 const meta = row.createDiv({ cls: 'ad-goal__meta' });
                 meta.createSpan({ text: g.current + (g.unit || '') + ' / ' + g.target + (g.unit || '') });
                 const btns = meta.createDiv({ cls: 'ad-goal__btns' });
-                const dec = btns.createEl('button', { text: '-' });
-                const inc = btns.createEl('button', { text: '+' });
-                dec.addEventListener('click', () => {
-                    g.current = Math.max(0, (g.current || 0) - 1);
-                    void saveAndRender();
+                // 自动统计类目标隐藏手动 ±，避免手动值与自动值互相覆盖
+                if (g.auto !== 'wordcount') {
+                    const dec = btns.createEl('button', { text: '-' });
+                    const inc = btns.createEl('button', { text: '+' });
+                    dec.setAttribute('aria-label', '减少 1');
+                    inc.setAttribute('aria-label', '增加 1');
+                    dec.addEventListener('click', () => {
+                        g.current = Math.max(0, (g.current || 0) - 1);
+                        void commit().then(renderList);
+                    });
+                    inc.addEventListener('click', () => {
+                        g.current = Math.min(g.target || 0, (g.current || 0) + 1);
+                        void commit().then(renderList);
+                    });
+                }
+                const editBtn = btns.createEl('button', { text: '\u2699' });
+                editBtn.title = '编辑目标';
+                editBtn.setAttribute('aria-label', '编辑目标');
+                editBtn.addEventListener('click', () => {
+                    new GoalEditModal(this.plugin.app, this.plugin, g, (next) => {
+                        const i = goals.findIndex((x) => x.id === g.id);
+                        if (i >= 0)
+                            goals[i] = next;
+                        void commit().then(() => this.refreshAutoGoals(goals, renderList, commit));
+                    }).open();
                 });
-                inc.addEventListener('click', () => {
-                    g.current = Math.min(g.target || 0, (g.current || 0) + 1);
-                    void saveAndRender();
+                const delBtn = btns.createEl('button', { text: '\u2715' });
+                delBtn.title = '删除目标';
+                delBtn.setAttribute('aria-label', '删除目标');
+                delBtn.addEventListener('click', () => {
+                    const i = goals.findIndex((x) => x.id === g.id);
+                    if (i >= 0)
+                        goals.splice(i, 1);
+                    void commit().then(renderList);
                 });
             }
+            const addRow = body.createDiv({ cls: 'ad-goal__add' });
+            const addBtn = addRow.createEl('button', { cls: 'ad-goal__addbtn', text: '+ 新增目标' });
+            addBtn.addEventListener('click', () => {
+                new GoalEditModal(this.plugin.app, this.plugin, null, (next) => {
+                    goals.push(next);
+                    void commit().then(() => this.refreshAutoGoals(goals, renderList, commit));
+                }).open();
+            });
         };
         renderList();
+        // 首屏先用持久缓存秒出，过期项交给后台重算，不阻塞渲染
+        void this.refreshAutoGoals(goals, renderList, commit);
     }
+    /** 统计指定文件夹（留空=整个库）下所有 md 的字数：中文按字计、英文按词计。
+     *  分批并发读取，避免上千个 Promise 一次性占满微任务队列。 */
+    async countWordsInFolder(folder) {
+        const prefix = (folder || '').trim().replace(/\/+$/, '');
+        const files = this.app.vault.getMarkdownFiles().filter((f) => {
+            // 排除依赖目录，避免第三方库里的 md 把字数统计严重放大
+            if (f.path.includes('node_modules/'))
+                return false;
+            if (!prefix || prefix === '/')
+                return true;
+            return f.path === prefix || f.path.startsWith(prefix + '/');
+        });
+        let total = 0;
+        const BATCH = 100;
+        for (let i = 0; i < files.length; i += BATCH) {
+            const chunk = files.slice(i, i + BATCH);
+            const counts = await Promise.all(chunk.map(async (f) => {
+                try {
+                    const raw = await this.app.vault.cachedRead(f);
+                    const body = raw.replace(/^---\r?\n[\s\S]*?\r?\n---\r?\n?/, '');
+                    const cjk = (body.match(/[\u4e00-\u9fa5]/g) || []).length;
+                    const en = (body.replace(/[\u4e00-\u9fa5]/g, ' ').match(/[A-Za-z0-9]+/g) || []).length;
+                    return cjk + en;
+                }
+                catch (e) {
+                    return 0;
+                }
+            }));
+            for (const n of counts)
+                total += n;
+        }
+        return total;
+    }
+    /** 刷新「自动统计」类目标：先套用持久缓存立即出结果，过期项交给后台重算。 */
+    async refreshAutoGoals(goals, onUpdated, commit) {
+        const auto = goals.filter((g) => g.auto === 'wordcount');
+        if (!auto.length)
+            return;
+        const cache = (this.plugin.settings.wcCache = this.plugin.settings.wcCache || {});
+        const now = Date.now();
+        let changed = false;
+        const stale = [];
+        for (const g of auto) {
+            const key = g.folder || '/';
+            const hit = cache[key];
+            if (!hit) {
+                stale.push(g);
+                continue;
+            }
+            if (hit.n !== g.current) {
+                g.current = hit.n;
+                changed = true;
+            }
+            if (now - hit.at > WC_TTL)
+                stale.push(g);
+        }
+        if (changed) {
+            if (commit)
+                await commit();
+            if (onUpdated)
+                onUpdated();
+        }
+        if (stale.length)
+            void this.recomputeWordCounts(stale, goals, cache, onUpdated, commit);
+    }
+    /** 后台重算（不阻塞首屏）：算完再回写并局部刷新。 */
+    async recomputeWordCounts(list, goals, cache, onUpdated, commit) {
+        let changed = false;
+        for (const g of list) {
+            const key = g.folder || '/';
+            const n = await this.countWordsInFolder(g.folder);
+            cache[key] = { at: Date.now(), n };
+            if (n !== g.current) {
+                g.current = n;
+                changed = true;
+            }
+        }
+        if (!changed)
+            return;
+        if (commit)
+            await commit();
+        else {
+            this.plugin.settings.goals = goals.map((g) => ({ ...g }));
+            await this.plugin.saveSettings();
+        }
+        if (onUpdated)
+            onUpdated();
+    }
     async renderFocusTime(board) {
         const card = this.getOrCreateCard(board, 'ad-card ad-b-focustime');
         this.cardHead(card, '\u23F1\uFE0F', t('home.modules.focusTime'), '');
@@ -12077,6 +12532,62 @@
         return { text, startLine: start };
     }
     /** 渲染一张映射卡；id='' 为占位卡（尚未配置任何映射） */
+    /** 打开计划任务面板（与「人生打卡清单」同款界面，数据源为各面板自己的 md）。 */
+    async showPlanBoard(id) {
+        await this.planBoard.show(id);
+    }
+    /** 自定义面板页：渲染面板绑定的 md 文档。
+     *  复用 renderMirrorCard 的原生渲染 —— 勾选写回、双链、图片、表格全部可用，
+     *  「打开源文件」按钮由 renderMirrorCard 自带。 */
+    async showCustomPanel(id) {
+        const cfg = (this.plugin.settings.customBoards ?? []).find((b) => b.id === id);
+        if (!cfg) {
+            this.showToast('面板不存在或已被删除', 'error');
+            return;
+        }
+        this.exitEditMode();
+        const board = this.boardEl;
+        if (!board)
+            return;
+        board.empty();
+        board.removeClass('ad-board');
+        board.removeClass('po-board');
+        board.removeClass('op-board');
+        board.removeClass('ck-board');
+        board.removeClass('im-board');
+        board.addClass('im-board');
+        this.currentPage = 'custom:' + id;
+        const container = board.createDiv({ cls: 'im-container' });
+        const head = container.createDiv({ cls: 'im-head' });
+        head.createEl('h2', { cls: 'im-title', text: (cfg.icon ? cfg.icon + ' ' : '') + cfg.name });
+        head.createDiv({ cls: 'im-sub', text: '在此记录或整理文档；勾选与编辑会写回源文件' });
+        const headBar = head.createDiv({ cls: 'im-panel__bar' });
+        const delBtn = headBar.createEl('button', { cls: 'im-btn im-panel__del', text: '移除面板' });
+        let armed = false;
+        delBtn.addEventListener('click', () => {
+            if (!armed) {
+                armed = true;
+                delBtn.textContent = '确认移除？';
+                delBtn.addClass('is-danger');
+                return;
+            }
+            void this.removeCustomPanel(id);
+        });
+        const card = container.createDiv({ cls: 'ad-card ad-b-mirror ad-panel__card' });
+        card.setAttribute('data-mirror-id', cfg.mirrorId);
+        await this.renderMirrorCard(container, cfg.mirrorId);
+    }
+    /** 移除自定义面板：删导航项与关联映射，源文件保留（面板页与「新建」页共用）。 */
+    async removeCustomPanel(id) {
+        const s = this.plugin.settings;
+        const b = (s.customBoards ?? []).find((x) => x.id === id);
+        s.customBoards = (s.customBoards ?? []).filter((x) => x.id !== id);
+        if (b)
+            s.mirrors = (s.mirrors ?? []).filter((m) => m.id !== b.mirrorId);
+        await this.plugin.saveSettings();
+        this.showToast('已移除面板（源文件保留）：' + (b ? b.name : ''));
+        void this.showDashboard();
+    }
     async renderMirrorCard(board, id) {
         const card = board.querySelector(`.ad-card.ad-b-mirror[data-mirror-id="${id}"]`);
         if (!card)
@@ -13167,7 +13678,8 @@
             this.settings.quickLinks = DEFAULT_QUICK_LINKS.map((l) => ({ ...l }));
             changed = true;
         }
-        if (!Array.isArray(this.settings.goals) || this.settings.goals.length === 0) {
+        // 只在字段缺失时初始化；保留用户主动清空（空数组）的结果
+        if (!Array.isArray(this.settings.goals)) {
             this.settings.goals = DEFAULT_GOALS.map((g) => ({ ...g }));
             changed = true;
         }
@@ -13187,7 +13699,7 @@
         if (typeof this.settings.importEnabled !== 'boolean')
             { this.settings.importEnabled = true; changed = true; }
         if (typeof this.settings.importTitle !== 'string' || !this.settings.importTitle)
-            { this.settings.importTitle = '导入'; changed = true; }
+            { this.settings.importTitle = '新建'; changed = true; }
         if (typeof this.settings.inspirationLimit !== 'number' || this.settings.inspirationLimit < 1)
             { this.settings.inspirationLimit = 20; changed = true; }
         if (!['day', 'week', 'quarter', 'year'].includes(this.settings.heatmapMode))
